{
  "name": "px-cdn",
  "version": "1.0.0",
  "description": "A module for rewriting template strings and px components CDN",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "pete.butler@ge.com",
  "dependencies": {
    "cdnizer": "^1.1.6",
    "fs-extra": "^0.30.0",
    "s3": "^4.4.0"
  },
  "devDependencies": {
    "vulcanize": "^1.14.8"
  }
}
